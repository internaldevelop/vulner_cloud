
# �����ױ����ò����ڴ˶���
myprops:
  defaultZone: http://localhost:10100/eureka/
  port: 10901

spring:
  application:
    name: fw_bend_server

server:
  port: ${myprops.port}

eureka:
  client:
    serviceUrl:
      defaultZone: ${myprops.defaultZone}
