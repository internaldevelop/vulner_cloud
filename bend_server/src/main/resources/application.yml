
# �����ױ����ò����ڴ˶���
myprops:
  defaultZone: http://localhost:10100/eureka/
  port: 10901

spring:
  application:
    name: fw_bend_server
  rabbitmq:
    host: 192.168.1.41
    port: 5672
    username: guest
    password: 123456
  cloud:
    bus:
      enabled: true
    trace:
      enabled: true

server:
  port: ${myprops.port}

eureka:
  client:
    serviceUrl:
      defaultZone: ${myprops.defaultZone}
